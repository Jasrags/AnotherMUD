name: tapestry-core
version: "0.1.0"
description: |
  Engine-scope starter pack — a small village and the wilderness path
  that leads to it. M2 content (data-only).
content:
  areas:
    - areas/*.yaml
  rooms:
    - rooms/*.yaml
  items:
    - items/*.yaml
  slots:
    - slots/*.yaml
  mobs:
    - mobs/*.yaml
  tracks:
    - tracks/*.yaml
  races:
    - races/*.yaml
  classes:
    - classes/*.yaml
  abilities:
    - abilities/*.yaml
  theme:
    - theme/*.yaml
  rarity:
    - rarity/*.yaml
  essence:
    - essence/*.yaml
  help:
    - help/*.yaml
  quests:
    - quests/*.yaml
  effects:
    - effects/*.yaml
  weather_zones:
    - weather_zones/*.yaml
  scripts:
    - scripts/*.lua
