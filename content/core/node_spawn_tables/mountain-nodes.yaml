# Node spawn table for the mountain biome (gathering.md §3.1). One stone
# outcrop spawns per foothill room (the same template the grassland uses);
# a worked-out one respawns on the area cadence.
id: mountain-nodes
entries:
  - { node: stone-outcrop, count: 1 }
