# Binaries
/anothermud
/bin/
/dist/
# air live-reload build dir (make watch)
/tmp/

# Go build/test artifacts
*.exe
*.test
*.out
*.prof
coverage.*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local tool state
.claude/settings.local.json

# Persistence root (default ANOTHERMUD_SAVE_DIR)
/saves/

# Generated analysis reports (codemap diffs, etc.)
/.reports/

# Nested unrelated project (separate embedded repo, not part of this module)
/tapestry

# Local symlink to a sibling engine repo (developer convenience, not part of this module)
/GoMudEngine

# Local symlink to the WheelMUD reference docs (WoT setting source, not part of this module)
/wot-reference
/telnet-smoke
